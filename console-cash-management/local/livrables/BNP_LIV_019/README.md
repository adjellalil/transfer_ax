# BNP_LIV_019 — ANALYSE_FLUX_MONEXT_MENSUELLE

| | |
|---|---|
| Script | `01.S8XPL.py` |
| Code | S8XPL |
| Statut | Refactoré CLI (argparse, sans GUI) — Session 2 |

Analyse du fichier MONEXT consolidé : RÉFÉRENTIEL CLIENT prioritaire (REF_RP > REF_RC > REF_IBAN),
5 types de PNB, 3 zones géo, worker vectorisé.

Paramètres et étapes : docstring en tête du script (ARGUMENTS CLI / DECOMPOSITION), aussi dans la Console.
